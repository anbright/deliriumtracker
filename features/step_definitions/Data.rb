Given (/^I have data$/) do
    #d = get "/api"
end

Then (/^the data should be nonnegative$/) do
    #d.each do |x|
     #   if x<0
      #      assert false
       # end
    #end
    #assert true
end

Then (/^the data should be less than the size of original data$/) do
    #n = avg.size
    #m= d.size
    
    #assert n<m
end